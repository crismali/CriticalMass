var React = require('react-native');
// var { Text, View, TouchableOpacity } = React;
var Navbar = require.requireActual('../navbar');

describe('Navbar', function() {
  it('is', function() {
    expect(Navbar).toBeTruthy();
  });
});
